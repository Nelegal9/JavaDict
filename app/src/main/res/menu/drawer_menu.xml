<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@id/termListFragment"
            android:title="@string/term_list"
            android:icon="@drawable/list"/>

        <item android:id="@id/favoriteTermListFragment"
            android:title="@string/favorite_term_list"
            android:icon="@drawable/checkbox_favorite"/>

        <item android:id="@id/settingsFragment"
            android:title="@string/settings"
            android:icon="@drawable/settings"/>

        <item android:id="@id/aboutFragment"
            android:title="@string/about"
            android:icon="@drawable/about"/>
    </group>
</menu>